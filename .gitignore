# Données générées (recréées par la CI à partir de templates/)
data/
output/
# NB : templates/ (gabarits Excel vides, format réel) est VERSIONNÉ — ne pas ignorer.
# Il permet de reconstituer toute la série fictive sur un clone neuf.

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# macOS
.DS_Store

# Claude Code (local)
.claude/

# Ignorer tous les fichiers ZIP (ex. exportés depuis Excel)
*.zip